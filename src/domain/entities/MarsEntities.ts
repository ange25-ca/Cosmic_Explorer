/*Se realiza la interfaz para los datos que se desea 
mostrar en la vista*/

//Se usaron los datos, más relevantes
export interface MarsClimate{
    //Los datos que mostrara son los siguientes:
    date: string;
    temperatura: number;
    temperaturaMin: number;
    temperaturaMax: number;
    viento:string;
}