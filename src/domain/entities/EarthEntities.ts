//Se crea la infaz de la API: EPIC
export interface Earth{
    //La información que mostrara
    caption: string,
    image: string,
    date:string //Por el momento es un string (pero la api establece que 
    //se debe construir)
}